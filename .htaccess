<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Legacy public verification/legal identity link now resolves to the CRM
    # regulatory notice page.
    RewriteRule ^corporate-identity-legal-independence/?$ /crm/regulatorynotice [L,R=301,NE]

    # Keep the public site on one canonical hostname so sessions and OAuth
    # callbacks don't bounce between www and non-www.
    RewriteCond %{HTTP_HOST} ^www\.uscapitalprivatebank\.com$ [NC]
    RewriteRule ^ https://uscapitalprivatebank.com%{REQUEST_URI} [L,R=301,NE]

    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
